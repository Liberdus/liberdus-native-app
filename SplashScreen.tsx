import React from "react";
import { View, StyleSheet } from "react-native";

const AnimatedSplash = () => <View style={styles.container} />;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    justifyContent: "center",
    alignItems: "center",
  },
});

export default AnimatedSplash;
